#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of Mutex.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'MutexArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

# class_name MutexArray

# var a:Array = [];

# func _crash(message: String):
#     assert(false, message)

# func _assert_Mutex(value: Mutex):
#     if (!value is Mutex):
#         _crash("value was expected to be of type Mutex")

# func _assert_not_null_Mutex(value: Mutex):
#     if (value == null):
#         _crash("value was expected to be not null")
#     _assert_Mutex(value)

# func set_at(position: int, value: Mutex):
#     _assert_Mutex(value);
#     a[position] = value;

# func get_at(position: int) -> Mutex:
#     return a[position];

# func add(value: Mutex):
#     _assert_Mutex(value);
#     a.append(value);

# Same as add, but with a different name.
# func append(value: Mutex):
#     _assert_Mutex(value);
#     a.append(value);

# func remove(position: int):
#     a.remove(position);

# func size() -> int:
#     return a.size();

# func clear():
#     a.clear();

# # func Contains(value: Mutex) -> bool:
# #     _assert_Mutex(value);
# #     return a.contains(value);

