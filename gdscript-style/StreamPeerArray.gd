#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of StreamPeer.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'StreamPeerArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

# class_name StreamPeerArray

# var a:Array = [];

# func _crash(message: String):
#     assert(false, message)

# func _assert_StreamPeer(value: StreamPeer):
#     if (!value is StreamPeer):
#         _crash("value was expected to be of type StreamPeer")

# func _assert_not_null_StreamPeer(value: StreamPeer):
#     if (value == null):
#         _crash("value was expected to be not null")
#     _assert_StreamPeer(value)

# func set_at(position: int, value: StreamPeer):
#     _assert_StreamPeer(value);
#     a[position] = value;

# func get_at(position: int) -> StreamPeer:
#     return a[position];

# func add(value: StreamPeer):
#     _assert_StreamPeer(value);
#     a.append(value);

# Same as add, but with a different name.
# func append(value: StreamPeer):
#     _assert_StreamPeer(value);
#     a.append(value);

# func remove(position: int):
#     a.remove(position);

# func size() -> int:
#     return a.size();

# func clear():
#     a.clear();

# # func Contains(value: StreamPeer) -> bool:
# #     _assert_StreamPeer(value);
# #     return a.contains(value);

