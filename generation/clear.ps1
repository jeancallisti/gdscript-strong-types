# Deletes all files generated by generate.ps1

$projectRootFolder = ".."

# Delete all files in output folders
Remove-Item -Path "$projectRootFolder/addons/array/*" -Force
Remove-Item -Path "$projectRootFolder/addons/dictionary/String/*" -Force
