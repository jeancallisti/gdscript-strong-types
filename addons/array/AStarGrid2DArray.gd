#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array
# in which the values are of type 'AStarGrid2D'.
# It is not meant to be attached to a Node (unless you want to!)
# You can generate more classes like this one for your own types.
class_name AStarGrid2DArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_AStarGrid2D(value: AStarGrid2D):
    if (!value is AStarGrid2D):
        _crash("value was expected to be of type AStarGrid2D")

func _assert_not_null_AStarGrid2D(value: AStarGrid2D):
    if (value == null):
        _crash("value was expected to be not null")

func set_at(position: int, value: AStarGrid2D):
    _assert_not_null_AStarGrid2D(value);
    _assert_AStarGrid2D(value);
    a[position] = value;

# Same as set_at but allows null
func set_at_nullable(position: int, value: AStarGrid2D):
    _assert_AStarGrid2D(value);
    a[position] = value;

func get_at(position: int) -> AStarGrid2D:
    return a[position];

func add(value: AStarGrid2D):
    _assert_not_null_AStarGrid2D(value);
    _assert_AStarGrid2D(value);
    a.append(value);

# Same as add but allows null
func add_nullable(value: AStarGrid2D):
    _assert_AStarGrid2D(value);
    a.append(value);

# Same as add, but with a different name.
func append(value: AStarGrid2D):
    _assert_not_null_AStarGrid2D(value);
    _assert_AStarGrid2D(value);
    a.append(value);

# Same as add_nullable, but with a different name.
func append_nullable(value: AStarGrid2D):
    _assert_AStarGrid2D(value);
    a.append(value);

func remove(position: int):
    a.remove(position);

func size() -> int:
    return a.size();

func clear():
    a.clear();

# func Contains(value: AStarGrid2D) -> bool:
#     _assert_AStarGrid2D(value);
#     return a.contains(value);

