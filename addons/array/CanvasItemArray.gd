#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array
# in which the values are of type 'CanvasItem'.
# It is not meant to be attached to a CanvasItem (unless you want to!)
# You can generate more classes like this one for your own types.
class_name CanvasItemArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_nullable_CanvasItem(value: CanvasItem):
    if (value == null):
        return

    if (!(value is CanvasItem)):
        _crash("value was expected to be of type CanvasItem")

func _assert_not_null_CanvasItem(value: CanvasItem):
    if (!(value is CanvasItem)):
        _crash("value was expected to be of type CanvasItem")

func _assert_all_nullable_CanvasItem(values: Array):
    for v in values:
        _assert_nullable_CanvasItem(v)

func _assert_all_not_null_CanvasItem(values: Array):
    for v in values:
        _assert_not_null_CanvasItem(v)

func set_at(position: int, value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a[position] = value;

# Same as set_at but allows null
func set_at_nullable(position: int, value: CanvasItem):
    _assert_nullable_CanvasItem(value);
    a[position] = value;

func get_at(position: int) -> CanvasItem:
    return a[position];

func add(value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.append(value);

# Same as add but allows null
func add_nullable(value: CanvasItem):
    _assert_nullable_CanvasItem(value);
    a.append(value);

# Same as add, but with a different name.
func append(value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.append(value);

# Same as add_nullable, but with a different name.
func append_nullable(value: CanvasItem):
    _assert_nullable_CanvasItem(value);
    a.append(value);

func remove(position: int):
    a.remove(position);

func size() -> int:
    return a.size();

# Same as size but different name
func length() -> int:
    return a.size();

func clear():
    a.clear();

# func Contains(value: CanvasItem) -> bool:
#     _assert_CanvasItem(value);
#     return a.contains(value);


func append_array(array: Array):
    _assert_all_not_null_CanvasItem(array);
    a.append_array(array);

func append_array_nullable(array: Array):
    _assert_all_nullable_CanvasItem(array);
    a.append_array(array);

func back() -> CanvasItem:
    return a.back();

# will implement later.
# For now, end-user must work with 'a' directly.
# int bsearch(value: Variant, before: bool = true)


# will implement later.
# For now, end-user must work with 'a' directly.
# int bsearch_custom(value: Variant, obj: Object, func: String, before: bool = true)

func count(value: CanvasItem) -> int:
    _assert_not_null_CanvasItem(value);
    return a.count(value);

func count_nullable(value: CanvasItem) -> int:
    _assert_nullable_CanvasItem(value);
    return a.count(value);

# will implement later.
# For now, end-user must work with 'a' directly.
# Array duplicate(deep: bool = false)

func is_empty() -> bool:
    return a.empty();

func erase(value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.erase(value);

func erase_nullable(value: CanvasItem):
    _assert_nullable_CanvasItem(value);
    a.erase(value);

# TODO
# func fill(value: CanvasItem)

func find(value: CanvasItem) -> int:
    _assert_not_null_CanvasItem(value);
    return a.find(value);

func find_nullable(value: CanvasItem) -> int:
    _assert_nullable_CanvasItem(value);
    return a.find(value);

#TODO
# func find_last(value: CanvasItem)

# Returns the first element or crashes if empty
func front() -> CanvasItem:
    if (a.empty()):
        _crash("Array is empty");
    return a.front();

# Same as front, but more familiar name
func first() -> CanvasItem:
    if (a.empty()):
        _crash("Array is empty");
    return a.front();

# Returns the first element or null if empty
func first_or_default() -> CanvasItem:
    if (a.empty()):
        return null;
    return a.front();

func has(value: CanvasItem) -> bool:
    _assert_not_null_CanvasItem(value);
    return a.has(value);

func has_nullable(value: CanvasItem) -> bool:
    _assert_nullable_CanvasItem(value);
    return a.has(value);

func hash() -> int:
    return a.hash();

func insert(position: int, value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.insert(position, value);

func insert_nullable(position: int, value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.insert(position, value);

#TODO
#func invert()

func max() -> CanvasItem:
    var result: CanvasItem = a.max();
    if (result == null): # cf. manual
        _crash("Cannot search max in array. Type is not comparable.");
    return result;

func min() -> CanvasItem:
    var result: CanvasItem = a.min();
    if (result == null): # cf. manual
        _crash("Cannot search min in array. Type is not comparable.");
    return result;

func pop_at(position: int) -> CanvasItem:
    if (a.size() == 0):
        _crash("Cannot pop from empty array")

    var result: CanvasItem = a.pop_at(position);
    if (result == null): # cf. manual
        _crash("Cannot pop from array. Position invalid");
    return result;

func pop_at_nullable(position: int) -> CanvasItem:
    if (a.size() == 0):
        _crash("Cannot pop from empty array")

    if (position < 0 or position >= a.size()):
        _crash("Cannot pop from array. Position invalid");

    return a.pop_at(position);

func pop_back() -> CanvasItem:
    if (a.size() == 0):
        _crash("Cannot pop from empty array")

    var result: CanvasItem = a.pop_back();
    if (result == null): # cf. manual
        _crash("Cannot pop from array (unexpected: null).");
    return result;

func pop_back_nullable() -> CanvasItem:
    if (a.size() == 0):
        _crash("Cannot pop from empty array")

    return a.pop_back();


func pop_front() -> CanvasItem:
    if (a.size() == 0):
        _crash("Cannot pop from empty array")

    var result: CanvasItem = a.pop_front();
    if (result == null): # cf. manual
        _crash("Cannot pop from array (unexpected: null).");
    return result;

func pop_front_nullable() -> CanvasItem:
    if (a.size() == 0):
        _crash("Cannot pop from empty array")

    return a.pop_front();


func push_back(value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.push_back(value);

func push_back_nullable(value: CanvasItem):
    _assert_nullable_CanvasItem(value);
    a.push_back(value);


func push_front(value: CanvasItem):
    _assert_not_null_CanvasItem(value);
    a.push_front(value);

func push_front_nullable(value: CanvasItem):
    _assert_nullable_CanvasItem(value);
    a.push_back(value);

func remove_at(position: int):
    if (position < 0 or position >= a.size()):
        _crash("Cannot remove from array. Position invalid");
    a.remove(position);

# TODO
# func resize(size: int)

#TODO
# func rfind(what: Variant, from: int = -1)

# TODO
# func shuffle()

func slice_shallow(begin: int, end: int):
    if (begin < 0 or begin >= a.size()):
        _crash("Cannot slice. 'begin' invalid");
    if (end < 0 or end >= a.size()):
        _crash("Cannot slice. 'end' invalid");
    if (end<begin):
        _crash("Cannot slice. 'begin' or 'end' invalid");
    return a.slice(begin, end);

func slice_deep(begin: int, end: int):
    if (begin < 0 or begin >= a.size()):
        _crash("Cannot slice. 'begin' invalid");
    if (end < 0 or end >= a.size()):
        _crash("Cannot slice. 'end' invalid");
    if (end<begin):
        _crash("Cannot slice. 'begin' or 'end' invalid");
    return a.slice(begin, end, 1, true);

# TODO: Check the behaviour with types that are not comparable.
func sort():
    a.sort();

#TODO
# func sort_custom(obj: Object, func: String)

#TODO
# func Array(from: PoolByteArray)
# func Array(from: PoolIntArray)
# func Array(from: PoolRealArray)
# func Array(from: PoolStringArray)
# func Array(from: PoolVector2Array)
# func Array(from: PoolVector3Array)
# func Array(from: PoolColorArray)

