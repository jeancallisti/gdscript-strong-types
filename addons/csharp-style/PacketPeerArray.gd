#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of PacketPeer.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'PacketPeerArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

class_name PacketPeerArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_PacketPeer(value: PacketPeer):
    if (!value is PacketPeer):
        _crash("value was expected to be of type PacketPeer")

func _assert_not_null_PacketPeer(value: PacketPeer):
    if (value == null):
        _crash("value was expected to be not null")
    _assert_PacketPeer(value)

func SetAt(position: int, value: PacketPeer):
    _assert_PacketPeer(value);
    a[position] = value;

func GetAt(position: int) -> PacketPeer:
    return a[position];

func Add(value: PacketPeer):
    _assert_PacketPeer(value);
    a.append(value);

# Same as Add, but with a different name.
func Append(value: PacketPeer):
    _assert_PacketPeer(value);
    a.append(value);

func Remove(position: int):
    a.remove(position);

func Size() -> int:
    return a.size();

func Clear():
    a.clear();

# func Contains(value: PacketPeer) -> bool:
#     _assert_PacketPeer(value);
#     return a.contains(value);

