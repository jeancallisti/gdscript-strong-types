#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of StreamPeerTLS.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'StreamPeerTLSArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

# class_name StreamPeerTLSArray

# var a:Array = [];

# func _crash(message: String):
#     assert(false, message)

# func _assert_StreamPeerTLS(value: StreamPeerTLS):
#     if (!value is StreamPeerTLS):
#         _crash("value was expected to be of type StreamPeerTLS")

# func _assert_not_null_StreamPeerTLS(value: StreamPeerTLS):
#     if (value == null):
#         _crash("value was expected to be not null")
#     _assert_StreamPeerTLS(value)

# func set_at(position: int, value: StreamPeerTLS):
#     _assert_StreamPeerTLS(value);
#     a[position] = value;

# func get_at(position: int) -> StreamPeerTLS:
#     return a[position];

# func add(value: StreamPeerTLS):
#     _assert_StreamPeerTLS(value);
#     a.append(value);

# Same as add, but with a different name.
# func append(value: StreamPeerTLS):
#     _assert_StreamPeerTLS(value);
#     a.append(value);

# func remove(position: int):
#     a.remove(position);

# func size() -> int:
#     return a.size();

# func clear():
#     a.clear();

# # func Contains(value: StreamPeerTLS) -> bool:
# #     _assert_StreamPeerTLS(value);
# #     return a.contains(value);

