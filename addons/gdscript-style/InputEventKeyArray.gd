#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of InputEventKey.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'InputEventKeyArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

# class_name InputEventKeyArray

# var a:Array = [];

# func _crash(message: String):
#     assert(false, message)

# func _assert_InputEventKey(value: InputEventKey):
#     if (!value is InputEventKey):
#         _crash("value was expected to be of type InputEventKey")

# func _assert_not_null_InputEventKey(value: InputEventKey):
#     if (value == null):
#         _crash("value was expected to be not null")
#     _assert_InputEventKey(value)

# func set_at(position: int, value: InputEventKey):
#     _assert_InputEventKey(value);
#     a[position] = value;

# func get_at(position: int) -> InputEventKey:
#     return a[position];

# func add(value: InputEventKey):
#     _assert_InputEventKey(value);
#     a.append(value);

# Same as add, but with a different name.
# func append(value: InputEventKey):
#     _assert_InputEventKey(value);
#     a.append(value);

# func remove(position: int):
#     a.remove(position);

# func size() -> int:
#     return a.size();

# func clear():
#     a.clear();

# # func Contains(value: InputEventKey) -> bool:
# #     _assert_InputEventKey(value);
# #     return a.contains(value);

