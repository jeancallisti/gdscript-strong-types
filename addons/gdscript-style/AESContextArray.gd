#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of AESContext.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'AESContextArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

# class_name AESContextArray

# var a:Array = [];

# func _crash(message: String):
#     assert(false, message)

# func _assert_AESContext(value: AESContext):
#     if (!value is AESContext):
#         _crash("value was expected to be of type AESContext")

# func _assert_not_null_AESContext(value: AESContext):
#     if (value == null):
#         _crash("value was expected to be not null")
#     _assert_AESContext(value)

# func set_at(position: int, value: AESContext):
#     _assert_AESContext(value);
#     a[position] = value;

# func get_at(position: int) -> AESContext:
#     return a[position];

# func add(value: AESContext):
#     _assert_AESContext(value);
#     a.append(value);

# Same as add, but with a different name.
# func append(value: AESContext):
#     _assert_AESContext(value);
#     a.append(value);

# func remove(position: int):
#     a.remove(position);

# func size() -> int:
#     return a.size();

# func clear():
#     a.clear();

# # func Contains(value: AESContext) -> bool:
# #     _assert_AESContext(value);
# #     return a.contains(value);

