#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of RefCounted.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'RefCountedArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

class_name RefCountedArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_RefCounted(value: RefCounted):
    if (!value is RefCounted):
        _crash("value was expected to be of type RefCounted")

func _assert_not_null_RefCounted(value: RefCounted):
    if (value == null):
        _crash("value was expected to be not null")
    _assert_RefCounted(value)

func SetAt(position: int, value: RefCounted):
    _assert_RefCounted(value);
    a[position] = value;

func GetAt(position: int) -> RefCounted:
    return a[position];

func Add(value: RefCounted):
    _assert_RefCounted(value);
    a.append(value);

# Same as Add, but with a different name.
func Append(value: RefCounted):
    _assert_RefCounted(value);
    a.append(value);

func Remove(position: int):
    a.remove(position);

func Size() -> int:
    return a.size();

func Clear():
    a.clear();

# func Contains(value: RefCounted) -> bool:
#     _assert_RefCounted(value);
#     return a.contains(value);

