#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of ResourceFormatSaver.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'ResourceFormatSaverArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

class_name ResourceFormatSaverArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_ResourceFormatSaver(value: ResourceFormatSaver):
    if (!value is ResourceFormatSaver):
        _crash("value was expected to be of type ResourceFormatSaver")

func _assert_not_null_ResourceFormatSaver(value: ResourceFormatSaver):
    if (value == null):
        _crash("value was expected to be not null")
    _assert_ResourceFormatSaver(value)

func SetAt(position: int, value: ResourceFormatSaver):
    _assert_ResourceFormatSaver(value);
    a[position] = value;

func GetAt(position: int) -> ResourceFormatSaver:
    return a[position];

func Add(value: ResourceFormatSaver):
    _assert_ResourceFormatSaver(value);
    a.append(value);

# Same as Add, but with a different name.
func Append(value: ResourceFormatSaver):
    _assert_ResourceFormatSaver(value);
    a.append(value);

func Remove(position: int):
    a.remove(position);

func Size() -> int:
    return a.size();

func Clear():
    a.clear();

# func Contains(value: ResourceFormatSaver) -> bool:
#     _assert_ResourceFormatSaver(value);
#     return a.contains(value);

