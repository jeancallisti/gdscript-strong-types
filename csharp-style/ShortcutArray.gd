#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of Shortcut.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'ShortcutArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

class_name ShortcutArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_Shortcut(value: Shortcut):
    if (!value is Shortcut):
        _crash("value was expected to be of type Shortcut")

func _assert_not_null_Shortcut(value: Shortcut):
    if (value == null):
        _crash("value was expected to be not null")
    _assert_Shortcut(value)

func SetAt(position: int, value: Shortcut):
    _assert_Shortcut(value);
    a[position] = value;

func GetAt(position: int) -> Shortcut:
    return a[position];

func Add(value: Shortcut):
    _assert_Shortcut(value);
    a.append(value);

# Same as Add, but with a different name.
func Append(value: Shortcut):
    _assert_Shortcut(value);
    a.append(value);

func Remove(position: int):
    a.remove(position);

func Size() -> int:
    return a.size();

func Clear():
    a.clear();

# func Contains(value: Shortcut) -> bool:
#     _assert_Shortcut(value);
#     return a.contains(value);

