#-----------------------------------------------------------------
#
#           THIS FILE WAS AUTOGENERATED.
#
#           Visit https://github.com/jeancallisti/gdscript-strong-types
#
#
#-----------------------------------------------------------------


# This class is a utility class meant to represent an array of JSON.
# it is not meant to be attached to a Node (unless you want to!)
# It exists in two versions (both named 'JSONArray'): The one in CSharp style (CapitalLetters) or the one in GDScript style (snake_case).
# Just uncomment the version you like the most.

class_name JSONArray

var a:Array = [];

func _crash(message: String):
    assert(false, message)

func _assert_JSON(value: JSON):
    if (!value is JSON):
        _crash("value was expected to be of type JSON")

func _assert_not_null_JSON(value: JSON):
    if (value == null):
        _crash("value was expected to be not null")
    _assert_JSON(value)

func SetAt(position: int, value: JSON):
    _assert_JSON(value);
    a[position] = value;

func GetAt(position: int) -> JSON:
    return a[position];

func Add(value: JSON):
    _assert_JSON(value);
    a.append(value);

# Same as Add, but with a different name.
func Append(value: JSON):
    _assert_JSON(value);
    a.append(value);

func Remove(position: int):
    a.remove(position);

func Size() -> int:
    return a.size();

func Clear():
    a.clear();

# func Contains(value: JSON) -> bool:
#     _assert_JSON(value);
#     return a.contains(value);

